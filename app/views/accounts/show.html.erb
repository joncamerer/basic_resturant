Show Path<br>

<%= "Linked account: #{@account}" %><br>

<% if @account != "blank" %>
  <%= "Logged in Member card no. : #{@card}" %><br>
  <% @account.each do |account| %>
    <%= "Account with same card no. : #{account.card_number}" %>
  <% end %>
<% else %>
  Sign Up!
<% end %>

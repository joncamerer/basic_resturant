<% if member_signed_in? %>
  <%= link_to "Sign Out", destroy_member_session_path, method: :delete, class: 'btn btn-default' %>
  <!--  TEMPORARY link, will eventually redirect to individual account page-->  
  <%= render "loyalty_account" %>
<% else %>
  <div class="container">
    <div class="row">
      <div class="col col-md-6 col-md-offset-3 text-center">
        <h2>Loyalty Program</h2>
        <h3>Sign in or sign up to view your loyalty accout!</p>
      </div>
      <div class="col col-md-4 col-md-offset-2">
        <div class="well text-center">
          <h3>Log in!</h3>
          <p> Log into your Maggie's loyalty acctount and </p>
          <%= link_to "Log In", new_member_session_path, class: 'btn btn-primary' %>
        </div>
      </div>
       <div class="col col-md-4">
        <div class="well text-center">
          <h3>Sign up!</h3>
          <p> Sign up for a Maggie's loyalty account today and find out what you've been missing!</p>
          <%= link_to "Sign Up", new_member_registration_path, class: 'btn btn-primary' %>
        </div>
      </div>
    </div>
  </div>
<% end %>